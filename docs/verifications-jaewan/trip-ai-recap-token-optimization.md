# AI Recap 이미지 토큰 최적화 검증 기록

## 비교 규칙

- 이슈 [#107](https://github.com/together-trip/togethertrip-server-main/issues/107)의 고정 16요청 세트를 모든 단계에서 반복한다.
- 단계별 코드, 모델, 크기, 품질, 참조 입력 조건을 함께 기록한다.
- 사용자 제공 32,423토큰은 기존 운영 관측값이며 fixture가 동일하다고 확인되지 않았으므로 참고값으로만 비교한다.
- 단계 간 개선율은 이 문서의 1단계 Spring AI 기준선 28,312토큰을 분모로 계산한다.
- 비용은 측정 시점의 `gpt-image-2` 단가인 텍스트 입력 $5/백만 토큰, 이미지 입력 $8/백만 토큰, 이미지 출력 $30/백만 토큰으로 추정한다.

## 1단계: Spring AI 계측과 기준선

### 구현

- Spring AI 2.0.0 `ImageModel` 기반 생성 경로를 도입했다.
- Spring AI 공식 OpenAI SDK/HTTP 전송 계층으로 편집 API를 보완했다.
- OpenAI 응답의 상세 usage를 Spring AI `ImageResponseMetadata`에 보존한다.
- 생성/편집, 모델, 크기, 품질, 참조 이미지 수, 성공 여부, 지연시간, 토큰과 실패 유형을 구조화 로그로 남긴다.
- 공식 SDK의 자동 재시도는 0으로 설정해 기준선에서 숨은 중복 호출을 배제했다.
- 실 API 테스트는 `OPENAI_LIVE_BENCHMARK_APPROVED=true`일 때만 실행된다.

### 실행 조건

| 항목 | 값 |
|---|---|
| 실행일 | 2026-07-22 |
| 요청 수 | 16 |
| 생성/편집 | 8 / 8 |
| 모델 | `gpt-image-2` |
| 크기 | `1152x2048` |
| 품질 | `medium` |
| 참조 이미지 | 편집당 256×256 PNG 1장 |
| 결과 경로 | `build/reports/openai-image-benchmark/stage-1-spring-ai-v2` |

### 결과

| 지표 | 1단계 기준선 |
|---|---:|
| 성공 | 16/16 (100%) |
| 총 토큰 | 28,312 |
| 요청당 토큰 | 1,769.50 |
| 입력 토큰 | 5,704 |
| 텍스트 입력 토큰 | 3,656 |
| 이미지 입력 토큰 | 2,048 |
| 캐시 입력 토큰 | 0 |
| 출력 토큰 | 22,608 |
| 예상 비용 | $0.712904 |
| p50 | 55,941ms |
| p95 | 72,422ms |
| 프롬프트 최소/p50/p95/최대 | 978 / 1,068 / 1,128 / 1,128자 |

사용자 제공 32,423토큰보다 4,111토큰(12.68%) 적지만 실행 fixture가 다르므로 Spring AI 전환 효과로 단정하지 않는다. 동일한 예비 fixture에서 WebClient와 Spring AI가 모두 28,082토큰을 반환한 것도 확인했으며, 전송 라이브러리 자체는 모델 토큰 사용량을 줄이는 수단이 아니라는 결론이다.

### 품질 판정

- 16장 모두 1152×2048이며 정확한 9:16이다.
- 16/16에서 여행지와 동행 여행 맥락이 유지되었다.
- PHOTO 장면은 사실적 사진, ILLUSTRATION 장면은 질감 있는 삽화로 구분되었다.
- 인물은 후면 또는 원거리로 표현되었고 식별 가능한 얼굴이나 얼굴 클로즈업이 없었다.
- 읽을 수 있는 텍스트, 로고, 워터마크가 없었다.
- 판정: 16/16 통과, 2단계 비교 기준으로 채택.

### 검증 명령

```bash
./gradlew test --tests 'com.togethertrip.main.triprecap.service.ai.OpenAiTripRecapTokenBenchmarkTest' --rerun-tasks --info
```

실행 시간은 15분 37초였고 빌드는 성공했다. 일반 단위 테스트는 API 키 없이 실행되며 live benchmark는 조건 불충족 시 건너뛴다.

저장소 전체 품질 게이트도 통과했다.

```bash
./gradlew check
```

최초 실행에서는 새 어댑터의 예외 분기 때문에 전체 branch coverage가 79%로 하한 80%에 미달했다. Spring AI 옵션 계약 실패 테스트를 보강한 뒤 기준을 낮추지 않고 재실행해 통합 테스트, 커버리지 검증, PIT를 포함한 `check`가 성공했다.

## 단계별 누적 비교

| 단계 | 총 토큰 | 1단계 대비 | 예상 비용 | 성공/품질 | 채택 여부 |
|---|---:|---:|---:|---|---|
| 1. Spring AI 기준선 | 28,312 | 기준 | $0.712904 | 16/16, 16/16 | 채택 |
| 2. 프롬프트 | - | - | - | - | 대기 |
| 3. 참조 입력 | - | - | - | - | 대기 |
| 4. 출력 프로파일 | - | - | - | - | 대기 |
| 5. 모델 라우팅 | - | - | - | - | 대기 |
| 6. 중복/캐시/재시도 | - | - | - | - | 대기 |
